# scripts/retrofit/

Utilitarios de **retrofit** — herramientas para aplicar mejoras del pipeline
de forma retroactiva sobre `data/items.jsonl` cuando el corpus histórico
no se beneficia automáticamente.

> **Estos scripts NO contienen lógica propia.** Solo importan funciones de
> `scripts/manga_watch.py` y las aplican a items ya guardados. Las mismas
> funciones ya corren automáticamente durante cualquier scrape nuevo
> (`bootstrap.sh`, `full_run.sh`).

## Cuadro completo de retrofits

| Script | Qué hace | Cuándo correrlo |
|---|---|---|
| `clean_titles.py` | Re-aplica `clean_title()` a títulos guardados (mojibake, prefijos junk). | Tras agregar pattern nuevo a `TITLE_JUNK_PATTERNS` en manga_watch.py. |
| `filter_non_manga.py` | Re-aplica `is_likely_manga` + `is_pure_novel` + `is_comic_not_manga`. Mueve los rechazados fuera de items.jsonl. | Tras agregar pattern a `_NON_MANGA_HARD`/`_SOFT` o `data/comics_blacklist.yml`. |
| `filter_collectible.py` | Re-aplica `is_collectible_edition` (segundo gate). Mueve tomos regulares fuera. | Tras cambios a `COLLECTIBLE_EDITION_SIGNAL_TYPES` o `is_collectible_edition`. ⚠️ Cuidado: este filtro rechaza items de referencia Mangavariant que SIEMPRE deben mantenerse — el skill `/watch-standardize-catalog` los preserva pero este retrofit los puede quitar erróneamente. |
| `rescore.py` | Recalcula `score` + `signal_types` + `product_type` para cada item. | Tras cambiar `detect_signals`, `score_candidate`, o un peso en `signal_score_table`. |
| `backfill_metadata.py` | Per-item HTTP fetch para rellenar campos vacíos (image_url, author, isbn, price, release_date) o el carrusel multi-imagen (`--only images`, popula `images[]` desde la galería del detail page). | Lento (network-bound). Cuando mejoraste `_extract_label_value_pairs`/`_extract_images_from_detail_soup` o agregaste extractor por fuente. La fase `[4e2]` de `scrape_full.sh` corre `--only images` automáticamente. |
| `backfill_cluster_key.py` | Pobla `cluster_key` en items históricos sin él. | Tras cambiar `derive_cluster_key` (raro). |
| `search_discovery.py` | NO es un retrofit puro — descubre items NUEVOS via Gemini/Tavily/DDG. Vive acá por proximidad. | 1×/semana o cuando querés ampliar el corpus sin esperar al overnight. |
| `wayback_recover.py` | Para items que dan 404/410, busca snapshot en archive.org y rescata cover/título/autor. | 1×/semana como mucho. Pesado (chequea miles de URLs). |
| `expand_whakoom_ediciones.py` | Convierte filas con URL Whakoom `/ediciones/<id>/<slug>` en N filas `/comics/<X>/<slug>/<vol>` (una por tomo). | Tras un search discovery que trajo `/ediciones/` desde Gemini. Idealmente: 0 filas `/ediciones/` residuales en el catálogo. Soporta one-shots via `/login?ReturnUrl=`. |
| `expand_index_pages.py` | Limpia páginas-índice guardadas como productos: Whakoom `/publisher/` (expande), Shopify multi-tomo variants (Dark Horse), `/blogs/news/` (elimina), `/collections/X` sin `/products/` (elimina). | Tras cada `search_discovery`. Idempotente. |
| `backfill_animeclick_details.py` | Para los items de AnimeClick ingestados sin `fetch_details=True` (release_date/price/description vacíos), fetcha directamente las páginas de detalle sin re-navegar el calendario AJAX. 4 workers, ~8 min para 1400 items. | 1× tras la ingesta inicial de AnimeClick o cuando aparezcan nuevos items sin fecha/precio. |
| `strip_legacy_cover_fields.py` | **Migración one-shot (2026-06-09): elimina los campos top-level `image_url`/`image_local` del item.** `images[0]` pasa a ser la única fuente de verdad de la portada. Rescata el `local` drifteado a `images[0]` cuando el archivo existe en disco, siembra `images[0]` donde faltaba, y borra los campos top-level. NO toca `sources[]` (otro layer). Con backup. Idempotente (2ª corrida = no-op). Flags: `--dry-run`. | 1× (ya corrido). Después: `mirror_images.py` para llenar los `local` faltantes de galería. |
| `mirror_images.py` | Espejo local (Image storage Fase 1). Backfill: descarga a `data/images/` el `local` faltante de CADA entry de `images[]` (portada `images[0]` + galería). GC mark-and-sweep: saca de `data/images/` los archivos que ningún item referencia — cuenta `images[].local` + `sources[].image_local` — (a cuarentena `_orphans/`, o `--gc-delete`). | 1× para bajar el catálogo histórico (el scrape ya baja las imágenes de items nuevos). Después, de vez en cuando para el GC. Idempotente. Ver "Image storage" en CLAUDE.md. |
| `upgrade_image_resolution.py` | Re-descarga portadas en resolución completa eliminando parámetros/segmentos CDN de resize. Detecta 9 patrones: Magento query params, WordPress -NxM, Shopify _Nx, Amazon ._SY300_., Rakuten ?_ex=, **Buscalibre fit-in/<W>x<H>/** (2-22×), **Cultura cdn-cgi/image/width=N/** (hasta 2×), **Whakoom small/thumb/medium → large** (3×), **Magento cache path** (requiere validación same_cover, ~20% imagen distinta). El download pasa Referer del item para evitar 403 anti-hotlink. Compara píxeles (umbral `--min-gain 0.10`). Actualiza `images[]` en items.jsonl; archivos viejos quedan como huérfanos para el próximo GC de `mirror_images.py`. Idempotente: URLs ya limpias no se reprocesan. **Automático**: corre como fase `[4g2]` de `scrape_full.sh` (después de `consolidate_sources`, antes de `dedup_carousel`). NO corre en el delta. | Automático en `scrape_full`. Manual tras ingestas grandes con imágenes pixeladas. Correr `mirror_images.py --gc` después para limpiar miniaturas huérfanas. |
| `backfill_prh_covers.py` | Para items EN con ISBN-13 de prefijo 978-0/978-1, prueba la URL determinística `images.penguinrandomhouse.com/cover/{isbn13}` del CDN de Penguin Random House. PRH distribuye manga EN de Dark Horse, Kodansha Comics, Seven Seas, Square Enix, TOKYOPOP, Titan, Inklore, Yen Press y más. 404 → descartado (magia-bytes). Valida ≥80 000 píxeles (descarta placeholders). Compara contra la imagen actual (umbral `--min-gain 0.10`). Deduplicado por ISBN: un solo download sirve a todos los items con ese ISBN. Actualiza `images[0]` (la portada). Idempotente: items ya usando PRH CDN no se retocan. | Tras ingestas de fuentes EN con imágenes pequeñas (Yen Press Calendar, VIZ, Manga-Sanctuary EN). 18 items mejorados en la corrida inicial (2026-05-28). |
| `upscale_images.py` | AI upscaling de portadas pixeladas usando waifu2x-ncnn-vulkan o realesrgan-ncnn-vulkan (modelos optimizados para anime/manga). Para imágenes con menos de `--max-pixels` píxeles (default 200 000 ≈ 450×445 px), aplica el upscaler ×2 y guarda el resultado como PNG lossless. Actualiza `image_local` en items.jsonl si la extensión cambia de .jpg → .png. Idempotente: saltea archivos ya procesados. Requiere instalar: `brew install waifu2x-ncnn-vulkan`. | Después de ingestas de fuentes JP (sumikko, booksprivilege, Rakuten) o IT (animeclick) que sólo exponen miniaturas ~150×220 px sin versión hi-res disponible en el servidor. |
| `fetch_better_covers.py` | Para items con imagen pequeña (< `--min-pixels`, default 100 000 px), busca portadas en mayor resolución por dos estrategias: (1) ISBN → lookup determinístico en Amazon CDN (`m.media-amazon.com/images/P/{isbn10}.09.SCLZZZZZZZ.jpg`) y PRH CDN (EN only); (2) sin ISBN → **Tavily Search API** con `include_images=True` que devuelve URLs de imagen directas sin fetchar páginas individuales (requiere `TAVILY_API_KEY` en `.env`, 1 000 queries/mes gratis; auto-detectado). Verificación doble: aspect ratio (±25%) + perceptual hash aHash 8×8 con distancia Hamming ≤ `--max-hash-dist` (default 12/64 bits) para confirmar que la candidata es la misma portada. Items con `variant_cover` o `retailer_exclusive` se saltan (web search devolvería la portada regular). Requiere Pillow: `.venv/bin/python3 -m pip install Pillow`. | Después de `upgrade_image_resolution.py` (que ya limpió parámetros CDN). Cuando hay muchos items con imagen < 100 000 px sin versión hi-res en el servidor origen — típico en AnimeClick IT (~1 000) y ListadoManga colecciones (~1 200). Correr primero con `--dry-run` para ver cuántos mejorarían. |
| `generate_slugs.py` | Genera el campo `slug` en items.jsonl para la ruta `/item/[slug]` del app Next.js (`web-next/`). Prioridad: `isbn:{X}` cluster_key → `{edition_key}-{vol}` → `{edition_key}` solo → `isbn-{isbn}` → `item-{sha1(url)[:12]}` fallback. Resuelve colisiones con sufijos `-b`/`-c` (el más antiguo conserva el slug limpio). Idempotente: solo actualiza `slug` si está vacío o si `edition_key`/`volume` cambió. Flags: `--dry-run`, `--only-missing`, `--verbose`. | Como **último paso del skill `/watch-standardize-catalog`** (después de escribir items.jsonl con sus campos canónicos). No corre automáticamente en el pipeline de scrape; solo cuando se estandarizan items nuevos. Ver docs/app/FRD-006-slug-generation.md. |
| `set_rarity.py` | Aplica el campo `rarity` (`common` / `rare` / `super_rare` / `ultra_rare`) a todos los items usando `derive_rarity_tier()` de manga_watch.py. Solo toca items sin valor asignado (o todos con `--force`). Respeta valores asignados por web-search (p. ej. `common` nunca se degrada) y NUNCA recalcula items con `rarity_verified_at` (ground truth de la skill watch-validate-rarity), ni siquiera con `--force` (override: `--include-verified`). Flags: `--dry-run`, `--force`, `--include-verified`. | Después de un scrape grande que agregó items nuevos sin `rarity`, o cuando se modifican las reglas de `derive_rarity_tier()`. |
| `translate_descriptions.py` | Popula `description_es` y `extras[].description_es` con la traducción al español de los campos `description` / `extras[].description`. Primario: **Google Translate** (gratis, sin API key). Opcional: **DeepL** (mejor calidad si `DEEPL_API_KEY` está en `.env`; plan gratuito = crédito único de 1M chars, no se renueva). `description` original no se modifica. Idempotente: salta items que ya tienen `description_es` (salvo `--force`). Requiere `pip install deep-translator langdetect`. Opcional: `pip install deepl`. | Después de cada scrape grande que agregó items con descripción en idioma extranjero. Sin clave DeepL igualmente funciona. |
| `apply_approvals.py` | Re-materializa el log durable `data/approvals.jsonl` (las cards aprobadas desde el dashboard — golden records) sobre `items.jsonl`. Reduce el log al estado final por `cluster_key` (last-wins) y re-aplica `approved_at`/`approved_by` (match por cluster_key, fallback url). Idempotente. Flags: `--dry-run`, `--approvals`, `--items`. | Tras **reconstruir `items.jsonl` de cero** (re-scrape/import) cuando se perdieron los `approved_at`. Las aprobaciones viven también en el JSONL, este log es el respaldo durable. Registrado en Panel de Control. |
| `fix_listadomanga_editions.py` | Enforce la regla "cada `/coleccion?id=N` = una edición" (gotcha #42) sobre items de listadomanga ya estandarizados: (a) split por id si un `edition_key` abarca >1 colección; (b) tomos `regular` con cofre/`from_extras` mal slugueados como special → `regular`; (c) colección artbook por título → `artbook`. Conservador (no re-deriva a ciegas). Recomputa cluster_key + consolida. Flags: `--dry-run`. **Sincronizado con el post-paso del skill `/watch-standardize-catalog`** (que aplica lo mismo automáticamente). | 1× para corregir el corpus histórico (el skill lo hace solo de ahí en más). Correr `generate_slugs.py` después. |
| `promote_hires_cover.py` | Promueve a `images[0]` la portada hi-res que ya existe en `images[1+]` (mismo cluster, fuente distinta), cuando la portada actual es un thumbnail <90 000 px. Verifica identidad con `_same_cover`. El thumbnail queda en la galería. Idempotente. Flags: `--dry-run`. | Tras ingestas de listadomanga-collections que dejaron thumbnails en portada pero el cluster ya tiene la hi-res de otra fuente. Correr `dedup_carousel_images.py` después si querés eliminar el dup. |
| `dedup_carousel_images.py` | Quita del carrusel (`images[]`) las imágenes que son LA MISMA portada en otra resolución, conservando la de MAYOR resolución (caso real: artbook con la cover hi-res del publisher + la misma cover como thumbnail de baja calidad de listadomanga). Hash perceptual aHash 8×8 con umbral Hamming ≤6 + aspect ratio ±12% para pares de tamaño comparable. **Caso thumbnail↔full** (gotcha #39: el thumb ~96×150 de listadomanga degrada tanto el aHash que sube a 7–14 bits vs su portada full — caso real cole=52): si una imagen es un thumbnail diminuto (lado ≤170px), la otra es ≥2× más grande y el aspect ratio es casi idéntico (±6%), umbral Hamming relajado a ≤14 (es inequívocamente la misma foto reescalada). **Solo toca kind=gallery** (los `extra` —cofres/tomos del box— son contenido curado, intocables) y exige dims válidas. Nunca deja un item sin imágenes; si la cover principal era una descartada, apunta a la kept hi-res. Flags: `--dry-run`, `--all` (default: solo items con alguna imagen de listadomanga). | **Automático**: corre como paso `[4h]` de `scrape_delta.sh`/`scrape_full.sh` (después de `consolidate_sources`, que es donde se crea el dup al unir imágenes de fuentes hermanas). Manual tras `fetch_better_covers.py`/`upgrade_image_resolution.py`. Registrado en Panel de Control. |
| `fix_listadomanga_title_collisions.py` | Desambigua títulos de DISPLAY que colisionan entre ediciones distintas de la misma obra (gotcha #42): dos ediciones con el mismo `title` (publishers/idiomas/años distintos) se veían idénticas aunque el `edition_key` las distingue. Apenda por prioridad: editorial → idioma → año → coleccion ("Saint Seiya Integral 1 (Planeta)" / "(Glénat)", "Death Note Black Edition 3 (2024)" / "(2013)"). Flags: `--dry-run`. **Sincronizado con el post-paso del skill `/watch-standardize-catalog`** (lo aplica solo). | 1× sobre el corpus; el skill lo mantiene. 56 títulos en la pasada inicial (2026-06-07). |
| `fix_listadomanga_titles.py` | Repara títulos a los que el parser VIEJO les dropeó el subtítulo (título en 2 líneas sin número: "CLAMP Art-book" + "North Side" → quedaba "CLAMP Art-book"; gotcha #41(g)). Re-parsea cada colección con el parser actual (alt-aware) y, si el título nuevo empieza con el viejo y es más largo (firma del subtítulo dropeado), actualiza title/title_standardized/title_original. Conservador: solo toca esos. Flags: `--dry-run`. | 1× sobre el corpus histórico (el parser ya quedó arreglado para futuras corridas). Corrigió 46 títulos en la pasada inicial (2026-06-07). |
| `fix_listadomanga_cross_covers.py` | Corrige portadas CRUZADAS entre ediciones distintas (cuando los títulos eran idénticos, un retrofit de cover-search asignó la misma hi-res a items de colecciones distintas — síntoma: mismo `image_local` en colecciones distintas). A cada item le devuelve SU propia portada de listadomanga (la del parser para su coleccion+volumen; fallback por volumen si el edition-kind cambió). Caso real: Ranma ½ Kanzenban EDT/Glénat 1553 vs Planeta 1978. Flags: `--dry-run`. | Cuando dos ediciones distintas muestran la misma portada. Tras `fix_listadomanga_titles.py` (que las hace distinguibles para search-covers). |
| `unmerge_listadomanga_editions.py` | Repara items que un `consolidate` histórico FUSIONÓ entre colecciones distintas (gotcha #42) cuando compartían el edition_key+volumen erróneo. Separa la fila en una por colección, atribuyendo la cover/precio/fecha de cada `source[]` y las imágenes del carrusel por archivo local / hash del disambiguador (el cofre de 1ª ed. vuelve a su tomo regular). Recomputa cluster_key por split. Flags: `--dry-run`. | 1× tras `fix_listadomanga_editions.py` si quedan items con `sources[]` de >1 colección. El `cluster_key` tier-2.5 (`lmc:`) evita que vuelva a pasar. Correr `generate_slugs.py` después. |
| `fix_raw_series_keys.py` | Re-deriva `series_key`/`edition_key` de los items RAW de listadomanga (sin `standardized_at`) con el heurístico arreglado (gotcha #43: `_normalize_series_name` dejaba el marcador `nº` como residuo `-no`). NO toca `title` ni `standardized_at` (los raw los procesa el skill LLM después). Tras re-derivar, consolida → los raw que ahora comparten cluster_key con su contraparte estandarizada deduplican. Flags: `--dry-run`. | 1× sobre el corpus tras corregir `_normalize_series_name`. 58 items en la pasada inicial (2026-06-07). El parser ya quedó arreglado para futuras corridas. Correr `generate_slugs.py` después. |
| `fix_store_publisher.py` | Saca el NOMBRE DE TIENDA del campo `publisher` (gotcha #44): las fuentes JP Sanyodo y Rakuten Books seteaban `publisher`=tienda, contaminando el `edition_key` (`…-rakuten-…`/`…-unknown-…`) y generando dups con la ficha de la editorial oficial. Limpia el campo `publisher` top-level y dentro de `sources[]`, recupera la editorial real por slug del edition_key o por hermano-ISBN (si no, `""`), y colapsa los dups de mismo-ISBN+mismo-series cuyo único conflicto era el slug de publisher (recomputa cluster_key + consolida). NO toca divergencias de romanización del `series_key` (trabajo del skill) ni aprobados. Idempotente. Flags: `--dry-run`. | 1× sobre el corpus histórico (las fuentes ya quedaron sin `publisher` de tienda para futuras corridas). 4 filas/2 grupos colapsados + 208 campos `publisher` limpiados en la pasada inicial (2026-06-07). Correr `generate_slugs.py` después. |
| `align_raw_to_std_coleccion.py` | Alinea items RAW a la edición ESTANDARIZADA de su MISMA `/coleccion` (regla coleccion=edición, gotcha #43). Re-scrapear una colección que ya tenía items estandarizados deja el raw nuevo con `edition_key`/`cluster_key` distinto del viejo → no consolidan y la colección aparece dos veces (ej. "Bastard!! nº1" raw vs "Bastard!! Deluxe 1" estandarizado, cole 1555). Los raw heredan `series_key`/`edition_key` del item estandarizado de su colección y consolidan (el estandarizado gana título/info; el raw aporta su fuente). NO toca `title` ni `standardized_at`. Idempotente. Flags: `--dry-run`. | **Automático**: paso `[4f2]` de `scrape_delta.sh`/`scrape_full.sh`, ANTES de `consolidate_sources`. Manual tras scrapear `listadomanga-collections` sobre colecciones ya conocidas. Registrado en Panel de Control. Correr `generate_slugs.py` después. |
| `fix_listadomanga_edition_display.py` | **`edition_display` = nombre OFICIAL de la edición, sin traducir (gotcha #49).** Re-fetchea el título de cada /coleccion (`_extract_collection_title`) y lo asigna como `edition_display` a todos sus items, reemplazando el slug genérico traducido ("Special (Norma Editorial)" → "Ataque a los Titanes"). Sólo el nombre del TOMO se traduce; el de la edición no. Network-bound (re-fetch ~500 colecciones, 6 workers). Flags: `--dry-run`, `--workers`. | 1× sobre el corpus histórico (el parser ya inyecta el título oficial en items nuevos). 507 colecciones / 1303 items en la pasada inicial (2026-06-07). |
| `fix_especial_title_order.py` | Normaliza el título de las EDICIONES ESPECIALES de TODAS las fuentes a "{serie} {vol} Edición Especial" (vol antes del calificador, sin paréntesis; gotcha #52). Usa `mw.format_especial_title` (sólo cambia títulos con patrón especial). Idempotente, respeta `approved_at`. Flags: `--dry-run`. | Paso 3a2 del enforcer. 1515 títulos en la pasada inicial. |
| `fix_lmc_display_titles.py` | Normaliza el título de display de los tomos de listadomanga (gotcha #52/#54): quita el marcador "nº" ("Atelier of Witch Hat nº5" → "… 5") y es AUTORITATIVO sobre el qualifier "Edición Especial" — lo apenda si el kind es especial/special (distingue del tomo regular del mismo volumen) y lo QUITA si el kind NO es especial (data stale de cofres reclasificados especial→regular, #53). El kind sale del `cluster_key` (`lmc:cole:kind:vol`, fiable también para la fila base sin `item=`). Idempotente, respeta `approved_at`. Flags: `--dry-run`. | Paso 3a del enforcer; o 1× tras un scrape. El parser ya lo aplica a items nuevos. 693 títulos en la pasada inicial; 240 stripped tras #53. |
| `fix_doble_sobrecubierta_falsepos.py` | Quita los items que entraron por el falso positivo "doble sobrecubierta → kanzenban" (gotcha #51): una doble sobrecubierta es cosmética (ediciones regulares), NO una Kanzenban. Re-parsea cada coleccion kanzenban con el parser arreglado y quita SÓLO los items cuyo volumen ya no califica — conservador, NO toca kanzenban genuinos (Slam Dunk, 20th Century Boys, Inuyasha…). Respeta `approved_at`. Flags: `--dry-run`. | 1× tras remover la regla del parser. 105 falsos positivos en la pasada inicial (Zetman, Hikaru no Go, Princess Jellyfish, Blue Giant…). Correr `generate_slugs.py` después. |
| `collapse_baseurl_tomos.py` | **Colapsa filas base-url phantom (gotcha #56).** Una fila con primaria `coleccion.php?id=N` (sin `item=`) y kind del edition_slug (`kanzenban:2`) duplica al tomo sintético `regular-2` (old-format vs new-format del mismo tomo) → un tomo aparece DOS veces. La fusiona (merge_cluster) en el sintético del mismo (cole, vol), preferentemente el `regular`. SÓLO phantoms puros (base-url SIN `item=` propio en sources): una base-url que SÍ tiene synthetic es un producto real (regular que coexiste con el especial — NO fusionar). Idempotente, respeta `approved_at`. | **Automático**: enforcer paso 3-1 (antes de los títulos). 30 phantoms en la pasada inicial. |
| `merge_crosssource_into_lmc.py` | **Fusiona ficha cross-source en su tomo lmc (gotcha #56).** La ficha de tienda (`edition:`, tier-1) y el tomo de listadomanga (`lmc:`, tier-0) NO se fusionan por la diferencia de tier aunque sean el mismo producto (mismo edition_key+vol+título). El lmc es canónico y absorbe la URL de tienda como source. Exige título idéntico (no fusiona variantes). Idempotente, respeta `approved_at`. | **Automático**: enforcer paso 3-2. 3 en la pasada inicial (Fruits Basket Collector 3, Blue Lock 26, Centuria 1). |
| `disambiguate_coleccion_editions.py` | **coleccion distinta = edición distinta (gotcha #57).** Si standardize asignó el MISMO `edition_key` a /coleccion distintas (Biomega Ultimate vs Master; Magic Knight Rayearth vs Rayearth 2) sus tomos del mismo vol salen duplicados. Inserta `-c{cole}` antes del país en cada item → cada cole su edición. Idempotente (skip si ya lleva `-cNNNN`). | **Automático**: enforcer paso 3-0 (tras unify). 124 items en la pasada inicial. |
| `dedup_synthetic_source.py` | **Dedup por fuente sintética compartida (gotcha #54).** Una fuente sintética de listadomanga (`coleccion.php?id=N&item=<kind>-<vol>-<hash>`) identifica UN producto físico. Si aparece como source en >1 fila, las fusiona (con `merge_cluster`) y re-fija primaria+`cluster_key` (externa/tienda→`edition:`, sólo-listadomanga→`lmc:` con la URL `item=`). La identidad SIEMPRE se cualifica por `id` de colección — el hash del `item=` NO es único entre colecciones (placeholder de portada reusado). Caso cross-source (Panini+listadomanga deja la fila `lmc:`-only viva) y representante base-url (AoT `regular:34` dup de `special:34`). Conservador: SALTA y loguea componentes multi-hash (packs especial+variant, filas sobre-mergeadas) para revisión manual. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: paso `3c` del enforcer (antes de `consolidate_sources`). 16 dups en la pasada inicial; 3 multi-hash saltados (I'm Not an Angel cole 52, Berserk Pack 41/2688, Berserk Maximum 21/2689). |
| `reconcile_lista_stale.py` | Quita del corpus los items de listadomanga que el parser ACTUAL ya NO genera como coleccionables (data de parsers viejos buggy: omnibus pelados, falsos kanzenban/artbook). Re-fetchea cada coleccion, calcula el set de `(kind,vol)` coleccionable (score≥30 + gate) y quita los items con synthetic URL cuyo `(kind,vol)` no está. Conservador: sólo si el fetch fue OK, respeta `approved_at`, no toca old-format sin `item=`. Flags: `--dry-run`. | 1× tras un cambio de parser que reduce captura. 122 SOBRANTES en la pasada inicial (beck/hajime-no-ippo/banana-fish omnibus, etc.). Correr `generate_slugs.py` después. |
| `canonicalize_edition_slugs.py` | **Slug de TIPO de edición determinístico (gotcha #69).** El LLM de standardize elegía el slug de tipo de forma inconsistente entre corridas (限定版 a veces "limited", a veces "special") y partía la MISMA edición en dos edition_keys hermanos. Re-aplica post-LLM la tabla término→slug `edition_slug_from_text()` de manga_watch.py sobre fuentes NO-listadomanga (los lmc los gobierna coleccion=edición) cuando el título trae evidencia textual de un tipo confundible (special/limited/collector/deluxe), y además ABSORBE las keys "hermanas" minoritarias SIN evidencia textual dentro de la key evidenciada del grupo cuando los tomos se solapan. Sin evidencia no toca nada (precisión > recall). Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 3c1. 345 keys re-canonicalizadas + 7 absorbidas en la pasada inicial (2026-06-11); pares con tomos solapados 38→27, EDSLUG=0. |
| `merge_duplicate_series.py` | **Fusiona series_keys partidas por variantes mecánicas del slug (gotcha #70).** La misma obra con 2-3 series_key ("the-apothecary-diaries" vs "apothecary-diaries"; apóstrofes; vocales largas romaji) por entradas canónicas DUPLICADAS en `series_aliases.yml`. Agrupa bajo `aggressive_series_norm()` (fuente única con el fallback de `canonical_series_key`); si los displays también colapsan, FUSIONA los duplicados del YAML (gana la key con más items; la perdedora pasa a alias), reescribe items (series_key, prefijo de edition_key, cluster_key) y encola los grupos sin canónica a `data/unmapped_series.jsonl` con campo `merged_from`. Los grupos con displays divergentes se reportan como ambiguos y NO se tocan. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 3c2. 25 grupos fusionados en la pasada inicial (20 del YAML; quedan 3395 canónicas); 15 ambiguos quedan como warning SERIESDUP para curación. |
| `normalize_edition_publishers.py` | **Unifica el campo `publisher` dentro de cada edición.** Filas de la MISMA edition_key con strings de editorial distintos ("Panini Comics" / "Planet Manga"; "Glenat" / "glénat manga"; kanji vs romaji) ensucian el dato y, en el tier fuzzy del cluster_key, pueden partir clusters. Por mayoría dentro de cada edition_key: todas las filas toman el string GANADOR — el más común cuyo `_publisher_slug()` coincide con la editorial embebida en la key; si ninguno coincide, el más común a secas. Re-deriva cluster_key de las tocadas y consolida. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 3c3. 90 ediciones unificadas en la pasada inicial (2026-06-11), PUBMIX=0. Relacionado: alias kanji 東京漫画社→tokyomangasha en el mapa de publishers. |
| `fix_edition_key_prefix.py` | **Re-alinea el prefijo de serie del edition_key con el series_key (gotcha #71).** El formato es `{series_key}-{pub}-{slug}-{país}[-cN]`, pero standardize dejaba el segmento de serie stale (truncado a 35 chars, alias/traducción vieja "pokemon-sol-luna" vs "pokemon-sun-moon", tokens duplicados). Delega en `manga_watch.rebuild_edition_key_prefix()`: parsea la cola desde la derecha (slug del allowlist `_KNOWN_EDITION_SLUGS`; publisher OBLIGATORIAMENTE reconocido en `_PUBLISHER_SLUG_MAP` — si no, no toca, para no mutilar "nxb-tre"→"tre") y compara el SEGMENTO de serie exacto. Conservador con keys MÁS específicas que el series_key: solo repara repetición mecánica o equivalencia bajo `aggressive_series_norm`; un distinguidor legítimo ("danganronpa-1-2-reload") NO se toca. Re-deriva cluster_key y consolida. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 3c4. 164+34 keys reparadas en dos pasadas (2026-06-11); quedan 11 no-parseables como warning EKPREFIX (keys legacy con slug fuera del allowlist — curación manual). |
| `fix_title_edition_words.py` | **Colapsa palabra de edición duplicada en el título + quita "Regular" sobrante (gotcha #72).** El generador/LLM producía "5 Elementos Artbook Artbook", "Trigun Maximum Maximum 2" y "Noragami Regular 27". Colapsa SOLO el vocabulario de ediciones duplicado consecutivo (nunca palabras arbitrarias — "Dead Dead Demon's Dededededestruction" es legítimo) y saca "Regular" cuando el slug de la edición es regular. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 3c5. 91 títulos corregidos en la pasada inicial (2026-06-11). Los prompts del skill/workflow ya traen el GUARD de nombre de serie + regla "regular sin calificador" para items nuevos. |
| `enforce_listadomanga_rules.py` | **AUTORIDAD FINAL de agrupación tras el skill de standardize.** El LLM NO decide la agrupación: este orquestador re-aplica determinísticamente, en orden, TODAS las reglas duras sobreescribiendo lo que el LLM haya dejado — `edition_display` oficial (#49, recuperado del `description` sin red), país=edición (#46), **anomalías de edition_key (paso 2b: panini-es→panini, xx→país)**, coleccion=edición (#48, incl. fichas de tienda cross-source), desambiguación de títulos, **slug de tipo de edición por término (#69, paso 3c1) + fusión de series duplicadas (#70, paso 3c2) + publisher unificado por edición (paso 3c3) + prefijo de serie del edition_key (#71, paso 3c4) + palabra de edición duplicada/"Regular" en títulos (#72, paso 3c5)** — desde estos pasos ya NO es solo-listadomanga: aplican a todas las fuentes —, **re-derivar cluster_key (paso 3b2, gotcha #55 — sin esto consolidate fusiona por el cluster viejo)**, dedup por fuente sintética (#54, paso 3c), consolidate, **paso 4b: re-corre `fix_lmc_display_titles` + `fix_especial_title_order` DESPUÉS de consolidate** (el merge de filas podía revivir un título contaminado ya limpiado y el enforcer necesitaba 2 pasadas para converger; con 4b converge en UNA), dedup de portadas, slugs. Idempotente (probado: 2 corridas → items.jsonl byte-idéntico). | **SIEMPRE después de `/watch-standardize-catalog`** (es el Step 6b del skill). También sirve para re-normalizar el corpus tras cualquier edición manual de ediciones. |
| `unify_coleccion_edition.py` | **Una /coleccion = UNA página de edición (gotcha #48).** Unifica el `edition_key` de TODOS los items de una coleccion (regular+especial+cofres+variantes **+ fichas de TIENDA cross-source** que la referencian vía `sources[]`) al de su edición BASE (`regular` si existe; si no, la predominante de país CONOCIDO, ej. Berserk→`omnibus`), para que la vista de edición los muestre juntos. Las variantes del MISMO volumen (regular-34 vs especial-34) NO se fusionan: las distingue el `cluster_key` tier-0 `lmc:{coleccion}:{kind}:{vol}`. Una ficha de tienda se asigna a una coleccion sólo si referencia UNA sola. **Los BOX SETS se separan a su propia edición** (slug `boxset`, gotcha #58): el base se calcula con los NO-box y los box-set van a `{serie}-{pub}-boxset-{país}` (pack/especial/variant SÍ conviven en la edición; box set no). Idempotente. Flags: `--dry-run`. | **Automático**: paso `[4f3]` / enforcer paso 3. 46 colecciones con split interno (2026-06-07) + fichas de tienda Berserk/Hellsing (cross-source). Correr `generate_slugs.py` después. |
| `fix_edition_key_anomalies.py` | Normaliza 2 anomalías del `edition_key` (validate_corpus COLED/PAIS): **(A)** `panini-es`→`panini` (el país va en el sufijo, no en el slug de editorial — artefacto stale de standardize); **(B)** país `-xx` (desconocido) → país real SÓLO si es seguro: source con country explícito, o editorial mono-país (norma/planeta/milkyway→es, pika/kana/glenat→fr, star/jpop→it, viz/yenpress→us). Editoriales multi-país (panini/kodansha) o `unknown` se quedan en `xx` (no inventamos país). Re-deriva cluster_key + consolida. Idempotente, respeta `approved_at`. Flags: `--dry-run`. | **Automático**: enforcer paso 2b (tras `fix_edition_country`, antes de `unify`). 25 normalizados en la pasada inicial (8 panini-es + 17 xx→país). |
| `fix_edition_country.py` | **Regla dura país=edición (gotcha #46).** Sufija el `edition_key` con el código de país de la edición (`…-{country_slug}`) para que dos mercados nunca compartan edición/cluster (Hunter×Hunter Panini mezclaba ES+IT bajo `hunter-x-hunter-panini-variant`). Recomputa cluster_key y consolida. El país es el de la EDICIÓN (`item.country`), no el de la tienda → NO escinde `sources[]` (una tienda IT puede revender la edición FR). Idempotente. Flags: `--dry-run`. | 1× sobre el corpus tras introducir la regla. 10323 items en la pasada inicial (2026-06-07). El motor (`derive_series_metadata`) ya hornea el país en items nuevos. Las fusiones cross-país reales (misma editorial matriz en 2 mercados) se separan a mano. Correr `generate_slugs.py` después. |
| `fix_publisher_unknown_edition_key.py` | Reemplaza el segmento `-unknown-` del `edition_key` por el slug REAL de la editorial cuando el item SÍ tiene `publisher` poblado (gotcha #45). Causa: la editorial no estaba en `_PUBLISHER_SLUG_MAP` al estandarizar (Norma, Planeta, Astiberri, Ponent Mon, Ediciones B, etc. → `unknown`). Recomputa `_publisher_slug`, reemplaza, recompute cluster_key y consolida (fusiona dups que sólo diferían en el slug `unknown` vs real). NO toca series ni edition. Idempotente. Flags: `--dry-run`. | 1× tras agregar editoriales al map / tras estandarizar items de editoriales nuevas. 703 items en la pasada inicial (2026-06-07). Correr `generate_slugs.py` después. |
| `consolidate_sources.py` | Modelo **1-fila-por-producto**: agrupa por `cluster_key` y colapsa las filas duplicadas del mismo producto (misma edición encontrada en varias fuentes con URLs distintas) en UNA sola fila con `sources[]` (todas las fuentes), imágenes union (portada canónica primera) y extras union. Delega en `manga_watch.consolidate_by_cluster` (la misma primitiva que usa `append_jsonl` al ingestar). Idempotente. | Después de `/watch-standardize-catalog` (reasigna edition_key → nuevos clusters), o si ves cards duplicadas del mismo producto. Corre como paso `[4g]` del pipeline. Registrado en Panel de Control. |
| `sc_validate.py` | Validador de candidatas del skill `watch-search-covers` — `_same_cover` AND-gate + `candidate_metadata_conflict`. Usado por el skill, no por el pipeline batch. Script PERMANENTE (`tests/test_sc_validate.py`). |
| `sync_cover_preview.py` | **Sincronización de la cola de portadas candidatas.** Refresca `old_url`/`old_image`/`old_pixels`/`current_images` de cada entry contra el item actual; poda candidatas `pending` cuya premisa ya no existe: portada ya ≥ 90 000 px, foto target desaparecida de la galería, foto target ya ok, o `new_url` igual a la portada actual. Las candidatas `approved`/`rejected` son intocables. Elimina entries cuyo slug no existe en el catálogo o que quedan sin candidatas. Función importable `sync_preview()`. Invocado automáticamente por `GET /api/cover-preview` al cargar el panel; también disponible como CLI con `--dry-run`. Tests: `tests/test_sync_cover_preview.py`. Flags: `--dry-run`. | Al cargar `cover-preview.html` (automático). Manual para auditoría o `--dry-run`. |
| `sync_cover_images.py` | Saneamiento integral de imágenes (contrapartida-de-datos del extractor, gotcha #31). Por item: **(1)** portada mala — si `image_url` es placeholder/banner (`visuel_defaut`, banner "Lista de Mangas" Panini MX) promueve la 1ª imagen real de `images[]` o la limpia (→ 📚); **(2)** `images[0]` == portada — re-sincroniza con `image_url`/`image_local` (bug 2026-06-02 Dark Horse: card y carrusel mostraban fotos distintas); **(3)** basura — elimina duplicados exactos (Panini `[cover,cover,cover]`), dups http/https, y BASURA por patrón de URL (KADOKAWA `top_bar_banner`/`bnr_*`, avatares AnimeClick `/bundles/accommon/`+`/avatar/`, íconos Funside `icona_lucchetto`, `nowprinting` e-hon, `adulte` manga-sanctuary, `/images/site/yomi/` otakucalendar, `img_star`) **O por ARCHIVO LOCAL** (`_compute_junk_local`: 0 bytes, <6KB píxeles/íconos, o el mismo archivo compartido por ≥4 OBRAS distintas = placeholder/banner reusado — con guard que NO marca portadas reales de obras fragmentadas en varios series_key); **(4)** productos relacionados — descarta galerías que son otros tomos/series (Star Comics `/fumetti-cover/thumbnail/`, Manga-Sanctuary `/objet/150/`), dejando solo la portada. Salta aprobados (`--include-approved`). Idempotente. Flags: `--dry-run`, `--include-approved`. | Cuando card y carrusel muestran fotos distintas, hay imágenes repetidas / banners / avatares, o carruseles con fotos de otras series. Registrado en Panel de Control. |

## ⚠️ Aprobaciones (golden records) — los retrofits las RESPETAN

Los items que el owner aprobó desde el dashboard llevan `approved_at` (ver
"Aprobación humana" en CLAUDE.md). **Todos los retrofits que reescriben campos
descriptivos** (`rescore`, `clean_titles`, `filter_non_manga`,
`filter_collectible`, `set_rarity`, `backfill_metadata`,
`translate_descriptions`) **saltean los items aprobados por defecto** — no los
re-derivan ni re-filtran. Los dos filtros (`filter_non_manga`,
`filter_collectible`) además SIEMPRE conservan un item aprobado (nunca lo
rechazan). Flag `--include-approved` para forzar el procesamiento de aprobados
cuando realmente lo querés. El guard usa `is_approved()` de `manga_watch.py`.
Si agregás un retrofit nuevo que reescribe metadata descriptiva, sumá el mismo
guard.

## Cuándo NO los necesitás

- **Scrape nuevo de una fuente que ya existe**: el pipeline ya aplica todo.
- **Nunca tocaste las reglas**: no hay nada nuevo que aplicar a items viejos.
- **Curación general post-scrape**: para eso están los **skills** (ver más
  abajo), no los retrofits.

## Retrofits vs Skills — cuándo cada uno

Los retrofits son **mecánicos** (regex/function-based) y rápidos.
Los skills (`.claude/skills/*.md`) son **LLM-driven**, más sofisticados,
y se usan para curación que requiere juicio (asignar series, decidir
si algo es manga, consolidar nombres multilingües).

| Tarea | Herramienta |
|---|---|
| "Quité un patrón mojibake del título" | retrofit `clean_titles.py` |
| "Agregué pattern non-manga ej. 'puzzle XL'" | retrofit `filter_non_manga.py` |
| "Aparecieron items con series_key vacío o crudo" | skill `/watch-standardize-catalog` |
| "Hay 5 series_keys distintos que son la misma obra en diferentes idiomas" | skill `/watch-enrich-series-aliases` |
| "Un manga apareció con título 'Spider-Man' por error de scrape" | feedback en dashboard + skill `/watch-standardize-catalog` |
| "Quiero ser dueño de las portadas / limpiar imágenes viejas" | retrofit `mirror_images.py` |
| "Scrapée items nuevos y quiero que tengan URL `/item/[slug]` en el app" | retrofit `generate_slugs.py` |
| "Cambié las reglas de rareza / hay items sin `rarity`" | retrofit `set_rarity.py` |

## Backups — dónde van y cómo funcionan

**Regla única: todos los backups se crean con `backup_and_rotate()` importada de `manga_watch.py`.
NUNCA con `cp ... /tmp/`, NUNCA con lógica propia en el script.**

```python
from manga_watch import backup_and_rotate

# Al inicio del script, ANTES del loop (una sola vez):
if not dry_run:
    backup_and_rotate(items_path, "nombre-del-script")
```

`backup_and_rotate(path, label, max_keep=3)` crea el backup en
`data/backups/<filename>/` (p. ej. `data/backups/items.jsonl/items.nombre-del-script-TIMESTAMP.bak`)
y elimina automáticamente los backups más viejos, conservando solo los 3 más recientes.

**¿Por qué importa?**
- `data/backups/` está en `.gitignore`. Si el backup va a otro lado (p. ej. raíz del repo),
  aparece en `git status` como untracked y no hay rotación automática.
- La lógica de rotación la tiene `backup_and_rotate()` — implementarla a mano en cada script
  garantiza que divergen (como pasó con `fetch_better_covers.py` que usaba `items_path.parent.parent / "backups"` → creaba `backups/` en la raíz del repo).

**Anti-patterns:**
- ❌ `cp data/items.jsonl /tmp/items.jsonl.bak-$(date)`
- ❌ `backup_dir = items_path.parent.parent / "backups" / ...` (hardcoded)
- ❌ Definir tu propia función `_backup_items()` en el script

Todos los retrofits existentes ya usan `backup_and_rotate()`. Si agregás uno nuevo, hacé lo mismo.

## Uso típico

```bash
# Cleanup mecánico
.venv/bin/python scripts/retrofit/clean_titles.py --dry-run    # preview
.venv/bin/python scripts/retrofit/clean_titles.py              # aplicar

.venv/bin/python scripts/retrofit/filter_non_manga.py --dry-run
.venv/bin/python scripts/retrofit/filter_non_manga.py

# Backfill HTTP-bound (lento)
.venv/bin/python scripts/retrofit/backfill_metadata.py --sleep 0.2
.venv/bin/python scripts/retrofit/backfill_metadata.py --only image_url --limit 50
.venv/bin/python scripts/retrofit/backfill_metadata.py --only images --limit 50  # carrusel multi-imagen

# Expansiones de páginas-índice (Whakoom, Shopify variants)
.venv/bin/python scripts/retrofit/expand_whakoom_ediciones.py --dry-run
.venv/bin/python scripts/retrofit/expand_index_pages.py --dry-run

# Espejo local de portadas (backfill + GC)
.venv/bin/python scripts/retrofit/mirror_images.py --dry-run
.venv/bin/python scripts/retrofit/mirror_images.py --limit 100   # probar
.venv/bin/python scripts/retrofit/mirror_images.py               # todo + GC

# Upgrade de resolución (Mangavariant -300x300, Panini ?width=222, etc.)
.venv/bin/python scripts/retrofit/upgrade_image_resolution.py --dry-run   # cuántas hay
.venv/bin/python scripts/retrofit/upgrade_image_resolution.py --limit 100 # probar
.venv/bin/python scripts/retrofit/upgrade_image_resolution.py --workers 8 # todo
# Luego correr mirror_images --gc para limpiar las miniaturas huérfanas

# Portadas EN via PRH CDN (Yen Press, VIZ, Manga-Sanctuary EN, etc.)
.venv/bin/python scripts/retrofit/backfill_prh_covers.py --dry-run      # candidatos
.venv/bin/python scripts/retrofit/backfill_prh_covers.py --workers 8    # todo
.venv/bin/python scripts/retrofit/backfill_prh_covers.py --min-gain 0   # sin umbral de píxeles

# AI upscaling de portadas pixeladas (sumikko, booksprivilege, Rakuten JP, animeclick)
# Instalar primero: brew install waifu2x-ncnn-vulkan
.venv/bin/python scripts/retrofit/upscale_images.py --dry-run            # cuántas hay
.venv/bin/python scripts/retrofit/upscale_images.py --limit 20           # probar
.venv/bin/python scripts/retrofit/upscale_images.py                      # todo (< 200k px)
.venv/bin/python scripts/retrofit/upscale_images.py --max-pixels 100000  # solo las más pequeñas

# Buscar portadas en mayor resolución (AnimeClick IT, ListadoManga colecciones, etc.)
# Requiere: .venv/bin/python3 -m pip install Pillow
.venv/bin/python scripts/retrofit/fetch_better_covers.py --dry-run --limit 30 --verbose  # ver candidatos
.venv/bin/python scripts/retrofit/fetch_better_covers.py --no-search      # solo ISBN CDN (rápido, seguro)
.venv/bin/python scripts/retrofit/fetch_better_covers.py                  # todo (ISBN + Tavily search, requiere TAVILY_API_KEY en .env)

# Slugs para el app Next.js (web-next/)
.venv/bin/python scripts/retrofit/generate_slugs.py --dry-run    # preview sin escribir
.venv/bin/python scripts/retrofit/generate_slugs.py --only-missing  # solo items sin slug (normal)
.venv/bin/python scripts/retrofit/generate_slugs.py --verbose    # con log de cada asignación

# Re-aplicar aprobaciones (golden records) tras reconstruir items.jsonl
.venv/bin/python scripts/retrofit/apply_approvals.py --dry-run   # cuántas se re-aplicarían
.venv/bin/python scripts/retrofit/apply_approvals.py             # re-materializa desde approvals.jsonl
```

Cada script llama a `backup_and_rotate(items_path, "nombre-script")` antes
de sobrescribir `data/items.jsonl`. Los backups van siempre a `data/backups/items.jsonl/`
(max 3, rotación automática). Ver sección "Backups" arriba.

## Estado actual (última corrida — 2026-05-24)

- Total items: **~4400** (después de filtros, standardization, aliases).
- Cobertura post-Mangavariant integration:
  - image_url: ~100%
  - image_local (espejo local, Image storage Fase 1): ~98.6%
    (el resto: image_url mal extraída — data: URI, ícono — o 404
    muerto; no re-descargable, queda con image_url como fallback)
  - series_key: 100% (todos los items tienen serie canónica)
  - edition_key: 100%
  - volume: ~79% (vacío para artbooks/cover-only/one-shots)
  - isbn: ~30% (las bajas son items Mangavariant sin ISBN por diseño)
  - price: ~40% (Mangavariant no expone precio)
  - author: ~30%

Si la cobertura de un campo retroactiva-detectable empeora notablemente
después de un scrape grande, es señal de que conviene correr el retrofit
correspondiente.
